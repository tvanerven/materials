nelmio_api_doc:
    documentation:
        info:
            title: Materials browser API
            description: REST API to request material browser database.
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/(?!doc$) # Accepts routes under /api except /api/doc
